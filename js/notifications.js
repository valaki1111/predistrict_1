function sendNotificationToAdmin(message) {
  // Ide jöhet a tényleges értesítés (pl. email, Telegram, Slack)
  console.log('Admin notification:', message);
}

module.exports = { sendNotificationToAdmin };
